/**
 * Runtime package version. The package smoke test verifies this value against
 * package.json so release drift fails before publication.
 */
export const BOUNDEDRELAY_VERSION = "0.1.0" as const;
